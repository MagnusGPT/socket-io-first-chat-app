### No database. Only sockets connection and simple UI.
## Node modules to install:
* Express
* Socket.io
## How to install:
Open up the terminal within the folder and type `npm install express socket.io`
## How to start:
Open up the terminal within the folder and type `node index.js`
